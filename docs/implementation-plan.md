# Sprache 100단계 구현 계획

완료된 항목은 `[x]`, 진행 중인 항목은 `[~]`, 대기 항목은 `[ ]`로 표시한다.

## 1. 제품 결정과 기준선

- [x] 001. GitHub 원격 저장소를 `origin`으로 연결한다.
- [x] 002. 빈 저장소와 기본 브랜치 상태를 확인한다.
- [x] 003. 한국어를 기준 언어로 확정한다.
- [x] 004. 영어·일본어를 첫 완성 언어로 확정한다.
- [x] 005. 독일어, 프랑스어·스페인어, 중국어 순의 후속 출시를 확정한다.
- [x] 006. 중국어는 간체 우선, 번체 확장 가능 구조로 확정한다.
- [x] 007. 언어별 입문 샘플 100개 전략을 확정한다.
- [x] 008. 가나·로마자·병음 읽기 보조를 확정한다.
- [x] 009. XP·레벨·연속 학습·배지 게임화를 확정한다.
- [x] 010. 로그인 전 체험, 저장·동기화 시 Google 연결 방식을 확정한다.

## 2. 개발환경과 저장소

- [x] 011. Flutter stable SDK와 Dart를 설치하고 버전을 고정한다.
- [x] 012. Android SDK, Java, 라이선스 상태를 검증한다.
- [x] 013. Visual Studio C++ Desktop 빌드 도구를 설치한다.
- [x] 014. Flutter Android·Windows 타깃 진단을 통과한다.
- [x] 015. 모노레포 디렉터리 구조를 생성한다.
- [x] 016. 루트 패키지 스크립트와 `.gitignore`를 작성한다.
- [x] 017. 프로젝트 `AGENTS.md`에 제품·코드·보안 규칙을 고정한다.
- [x] 018. 환경변수 예제와 비밀값 제외 규칙을 작성한다.
- [x] 019. 프로젝트 개요 README를 작성한다.
- [x] 020. 100단계 실행 체크리스트를 저장소에 기록한다.

## 3. API와 Railway 기반

- [x] 021. Fastify TypeScript API 패키지를 생성한다.
- [x] 022. 환경변수 스키마와 안전한 설정 로더를 구현한다.
- [x] 023. Prisma PostgreSQL 스키마를 작성한다.
- [x] 024. `account_drive_bindings` 저장소 계층을 구현한다.
- [x] 025. `GET /health`를 구현한다.
- [x] 026. Google ID Token 검증 어댑터를 구현한다.
- [x] 027. Google `sub` HMAC-SHA256 계정 키 생성을 구현한다.
- [x] 028. `POST /v1/auth/verify`를 구현한다.
- [x] 029. `GET /v1/me/drive-root`를 구현한다.
- [x] 030. `PUT /v1/me/drive-root`를 구현한다.
- [x] 031. `DELETE /v1/me/drive-root`를 구현한다.
- [x] 032. 요청 검증, rate limit, 보안 헤더와 오류 응답을 구현한다.
- [x] 033. API 단위·통합 테스트를 작성한다.
- [x] 034. Dockerfile과 로컬 PostgreSQL 구성을 작성한다.
- [x] 035. Railway 배포 및 상태 확인 절차를 문서화한다.

## 4. 공통 다국어 도메인

- [x] 036. BCP 47 기반 `LanguageTag`와 `LanguageProfile`을 정의한다.
- [x] 037. 한국어↔학습 언어 코스 모델을 정의한다.
- [x] 038. 단어·문장 공통 `LearningItem` 모델을 정의한다.
- [x] 039. 가나·로마자·병음 `Reading` 모델을 정의한다.
- [x] 040. 명시적 문장 토큰과 문제 지원 능력 모델을 정의한다.
- [x] 041. 언어별 정답 정규화 파이프라인을 구현한다.
- [x] 042. 명시적 허용 답안과 제한적 오타 판정을 구현한다.
- [x] 043. 반복학습 상태와 응답 등급 모델을 구현한다.
- [x] 044. 10분·1일·3일 학습 단계와 간격 계산을 구현한다.
- [x] 045. mastered 전환과 lapse 처리를 구현한다.
- [x] 046. 결정적인 언어별 일일 학습 큐를 구현한다.
- [x] 047. 복습·오답·사용자 우선순위·신규 항목 정렬을 구현한다.
- [x] 048. XP 계산과 언어별 일일 목표를 구현한다.
- [x] 049. 계정 전체 레벨·연속 학습일·배지를 구현한다.
- [x] 050. 도메인 전체 단위 테스트를 작성한다.

## 5. Local-First 저장과 콘텐츠

- [x] 051. Drift 데이터베이스와 마이그레이션 기반을 구성한다.
- [x] 052. 콘텐츠·진도·이벤트·세션·설정 테이블을 구현한다.
- [x] 053. 동기화 상태·대기열·가져오기 테이블을 구현한다.
- [x] 054. 필요한 조회 인덱스를 구현한다.
- [x] 055. 영어 검증용 단어·문장 100개를 준비한다.
- [x] 056. 일본어 검증용 단어·문장 100개와 읽기를 준비한다.
- [x] 057. 콘텐츠 출처·라이선스·버전 메타데이터를 기록한다.
- [x] 058. 최초 실행에서 내장 스타터 카탈로그를 로드한다.
- [x] 059. CSV·JSON·JSONL 파서와 행별 검증을 구현한다.
- [x] 060. UUIDv5 중복 판정, 미리보기, 병합을 구현한다.

## 6. Flutter 셸과 플랫폼 UI

- [x] 061. Flutter 앱, Riverpod, go_router 기본 구조를 생성한다.
- [x] 062. 공통 디자인 토큰과 접근성 기준을 구현한다.
- [x] 063. 로그인 없는 체험 온보딩과 코스 선택을 구현한다.
- [x] 064. 언어별 홈, 목표, 복습·신규·취약 수치를 구현한다.
- [x] 065. Android 하단 내비게이션과 게임형 홈을 구현한다.
- [x] 066. Windows 좌측 사이드바와 업무형 홈을 구현한다.
- [x] 067. Windows 최소·일반·확장 크기 반응형 레이아웃을 구현한다.
- [x] 068. 작은 창에서도 빠른 학습이 가능한 compact 모드를 구현한다.
- [x] 069. XP, 레벨, 연속 학습, 배지 피드백을 구현한다.
- [x] 070. 플랫폼별 반응형 레이아웃과 위젯 테스트를 작성한다.

## 7. 학습과 퀴즈

- [x] 071. 공통 학습 세션 상태 전이를 구현한다.
- [x] 072. 외국어→한국어 객관식 단어 문제를 구현한다.
- [x] 073. 한국어→외국어 입력 문제를 구현한다.
- [x] 074. 문맥 빈칸 문제를 구현한다.
- [x] 075. 문장 의미 선택 문제를 구현한다.
- [x] 076. 명시적 토큰 기반 문장 배열 문제를 구현한다.
- [x] 077. 문장 입력·선택·빈칸 문제를 구현한다.
- [x] 078. 플랫폼 기본 TTS와 언어별 locale 선택을 구현한다.
- [x] 079. 즉시 정답 피드백과 반복 재출제를 구현한다.
- [x] 080. Windows Enter·1~4·Space·Esc·Ctrl+K 단축키를 구현한다.

## 8. Google Drive 동기화

- [x] 081. Mock 사용자와 가상 Drive 동기화 어댑터를 구현한다.
- [x] 082. 인증과 Drive 권한을 분리한 계정 상태 모델을 구현한다.
- [x] 083. Android Credential Manager 로그인 브리지를 구현한다.
- [x] 084. Android AuthorizationClient Drive 권한·Picker 브리지를 구현한다.
- [x] 085. Windows 시스템 브라우저 PKCE·loopback 인증을 구현한다.
- [x] 086. `drive.file` Picker 폴더 선택과 Folder ID 검증을 구현한다.
- [x] 087. `WordStudyData` 폴더와 manifest 초기화를 구현한다.
- [x] 088. snapshot pull·스키마 검증·SQLite 병합을 구현한다.
- [x] 089. 세션 단위 push와 지수 백오프 재시도를 구현한다.
- [x] 090. updatedAt·eventId·deviceId·tombstone 충돌 병합을 구현한다.

## 9. 검증과 배포

- [x] 091. 권한 취소·폴더 삭제·manifest 손상 복구를 구현한다.
- [x] 092. 지원하지 않는 원격 스키마 덮어쓰기 방지를 구현한다.
- [x] 093. 토큰·개인정보 로그 누출 정적 점검을 수행한다.
- [x] 094. 전체 Dart 분석·단위·위젯 테스트를 통과한다.
- [x] 095. 전체 API lint·단위·통합 테스트를 통과한다.
- [x] 096. Mock Mode Android APK를 빌드하고 실행 검증한다.
- [x] 097. Mock Mode Windows EXE를 빌드하고 크기 조절을 검증한다.
- [x] 098. 실제 Google·Drive·Railway 설정 문서를 완성한다.
- [x] 099. GitHub Actions에서 API·Flutter 검증을 자동화한다.
- [x] 100. 산출물, 실행 명령, 알려진 외부 자격증명 제약을 최종 보고한다.
